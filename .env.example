# 이 파일을 복사해서 .env 로 이름을 바꾸고, 실제 값을 채워넣으세요.

# OpenAI API 키
OPENAI_API_KEY=sk-여기에_본인_키_입력

# Firebase 서비스 계정 키 JSON 파일 경로 (로컬 실행 시)
FIREBASE_SERVICE_ACCOUNT_JSON=serviceAccountKey.json

# 프론트엔드 도메인 (쉼표로 여러 개 가능, 로컬 테스트는 * 로 열어둬도 됨)
ALLOWED_ORIGINS=*
